name: openapi
version: 1.0.0
description: OpenAPI API client
homepage: homepage

resolution: workspace

environment:
  sdk: ^3.6.1

dependencies:
  dio:
  equatable:
  json_annotation:

dev_dependencies:
  build_runner:
  json_serializable:
  test:
